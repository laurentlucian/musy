generator client {
  provider = "prisma-client"
  runtime  = "cloudflare"
  output   = "../../app/generated/prisma/client"
}

datasource db {
  provider = "sqlite"
  url      = "file:./prisma/db.sqlite"
}
