generator client {
  provider   = "prisma-client"
  engineType = "client"
  runtime    = "cloudflare"
  output     = "../../app/lib/services/db/generated.server"
}

datasource db {
  provider = "sqlite"
  url      = "file:./prisma/db.sqlite"
}
